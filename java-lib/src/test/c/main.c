#include <stdio.h>
#include <stdlib.h>
int main(int argc, char **argv) {
    fprintf(stderr, "Hallo Welt");
}

// if (argc != 2) {
//     fprintf(stderr, "Usage: %s <filter>\n", argv[0]);
//     exit(1);
// }
